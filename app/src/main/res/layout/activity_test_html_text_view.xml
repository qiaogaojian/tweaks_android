<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/gl_top"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0.27" />

        <ImageView
            android:id="@+id/iv_bg"
            android:layout_width="870pt"
            android:layout_height="398pt"
            android:background="@color/code_black"
            android:clickable="true"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="@id/gl_top" />

        <TextView
            android:id="@+id/tv_html_text"
            android:layout_width="0pt"
            android:layout_height="wrap_content"
            android:gravity="left|center"
            android:paddingLeft="180pt"
            android:paddingRight="180pt"
            android:textColor="@color/code_white"
            android:textSize="42pt"
            android:typeface="@{@string/typeface}"
            app:layout_constraintBottom_toBottomOf="@id/iv_bg"
            app:layout_constraintLeft_toLeftOf="@id/iv_bg"
            app:layout_constraintRight_toRightOf="@id/iv_bg"
            app:layout_constraintTop_toTopOf="@id/iv_bg"
            tools:text="是否花费 向MMM 赠送礼物？" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>