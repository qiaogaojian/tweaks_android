<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="TagGroup">
        <!-- Whether the tag group is in append mode. -->
        <attr name="atg_isAppendMode" format="boolean" />
        <!-- If the tag group is in append mode, what the hint of input tag. -->
        <attr name="atg_inputHint" format="string" />

        <!-- The tag view outline border color. -->
        <attr name="atg_borderColor" format="color" />
        <!-- The tag view text color. -->
        <attr name="atg_textColor" format="color" />
        <!-- The tag view background color. -->
        <attr name="atg_backgroundColor" format="color" />

        <!-- The dash outline border color, when in append mode. -->
        <attr name="atg_dashBorderColor" format="color" />
        <!-- The input tag hint text color, when in append mode. -->
        <attr name="atg_inputHintColor" format="color" />
        <!-- The input tag type text color, when in append mode. -->
        <attr name="atg_inputTextColor" format="color" />

        <!-- The checked tag view outline border color. -->
        <attr name="atg_checkedBorderColor" format="color" />
        <!-- The checked text color. -->
        <attr name="atg_checkedTextColor" format="color" />
        <!-- The checked marker color. -->
        <attr name="atg_checkedMarkerColor" format="color" />
        <!-- The checked tag view background color. -->
        <attr name="atg_checkedBackgroundColor" format="color" />

        <!-- The tag view background color, when the tag view is being pressed. -->
        <attr name="atg_pressedBackgroundColor" format="color" />

        <!-- The tag view outline border stroke width. -->
        <attr name="atg_borderStrokeWidth" format="dimension" />
        <!-- The tag view text size. -->
        <attr name="atg_textSize" format="dimension" />

        <attr name="atg_horizontalSpacing" format="dimension" />
        <attr name="atg_verticalSpacing" format="dimension" />
        <attr name="atg_horizontalPadding" format="dimension" />
        <attr name="atg_verticalPadding" format="dimension" />
    </declare-styleable>

    <declare-styleable name="Themes">
        <attr name="tagGroupStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="CustomCancelView">
        <attr name="view_radius" format="dimension" />
        <attr name="view_color" format="color" />
    </declare-styleable>

    <declare-styleable name="SwitchButton">
        <attr name="uncheckSrc" format="reference"></attr>
        <attr name="checkSrc" format="reference"></attr>
        <attr name="boardWidth" format="dimension"></attr>
        <attr name="uncheckRevealColor" format="color"></attr>
        <attr name="checkRevealColor" format="color"></attr>
    </declare-styleable>

    <declare-styleable name="CustomHeadImageView">
        <attr name="headSrc" format="string"></attr>
        <attr name="frameSrc" format="string"></attr>
        <attr name="frameTopSrc" format="string"></attr>
        <attr name="userNo" format="string"></attr>
    </declare-styleable>

    <declare-styleable name="MarqueeTextView">
        <attr name="scroll_interval" format="integer" />
        <attr name="scroll_first_delay" format="integer" />
        <attr name="scroll_mode">
            <enum name="mode_forever" value="100" />
            <enum name="mode_once" value="101" />
        </attr>
    </declare-styleable>

    <!-- 飘星自定义属性 -->
    <declare-styleable name="HeartLayout">
        <attr name="initX" format="dimension" />
        <attr name="initY" format="dimension" />
        <attr name="xRand" format="dimension" />
        <attr name="animLengthRand" format="dimension" />
        <attr name="xPointFactor" format="dimension" />
        <attr name="animLength" format="dimension" />
        <attr name="heart_width" format="dimension" />
        <attr name="heart_height" format="dimension" />
        <attr name="bezierFactor" format="integer" />
        <attr name="anim_duration" format="integer" />
    </declare-styleable>

    <declare-styleable name="NumberProgressBar">
        <attr name="progress_current" format="integer" />
        <attr name="progress_max" format="integer" />

        <attr name="progress_unreached_color" format="color" />
        <attr name="progress_reached_color" format="color" />

        <attr name="progress_reached_bar_height" format="dimension" />
        <attr name="progress_unreached_bar_height" format="dimension" />

        <attr name="progress_text_size" format="dimension" />
        <attr name="progress_text_color" format="color" />

        <attr name="progress_text_offset" format="dimension" />

        <attr name="progress_text_visibility" format="enum">
            <enum name="visible" value="0" />
            <enum name="invisible" value="1" />
        </attr>
    </declare-styleable>

    <!--<declare-styleable name="Themes">-->
    <!--<attr name="numberProgressBarStyle" format="reference"/>-->
    <!--</declare-styleable>-->

    <declare-styleable name="LeanTextView">
        <attr name="degree" format="integer" />
        <attr name="skew_degree" format="integer" />
    </declare-styleable>

    <declare-styleable name="ParallelogramImageView">
        <attr name="degrees" format="integer" />
    </declare-styleable>

    <declare-styleable name="ScrollTextView">
        <attr name="clickEnable" format="boolean" />
        <attr name="isHorizontal" format="boolean" />
        <attr name="speed" format="integer" />
        <attr name="text" format="string" />
        <attr name="text_color" format="color" />
        <attr name="text_size" format="dimension" />
        <attr name="times" format="integer" />

        <attr name="isScrollForever" format="boolean" />
    </declare-styleable>

    <declare-styleable name="ClipConstraintLayout">
        <attr name="hasPadding" format="boolean" />
    </declare-styleable>

    <declare-styleable name="TestLinearGradientShaderView">
        <attr name="gradient_text" format="string" />
        <attr name="gradient_text_size" format="integer" />
        <attr name="gradient_width" format="integer" />
    </declare-styleable>

    <declare-styleable name="MaskLayout">
        <attr name="side_padding" format="integer" />
        <attr name="isHorizon" format="boolean" />
    </declare-styleable>

    <declare-styleable name="CircleView">
        <attr name="radius" format="integer" />
        <attr name="color" format="color" />
    </declare-styleable>
</resources>
